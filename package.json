{
  "name": "iterm_quotes",
  "version": "1.0.2",
  "description": "simple quotes generator whenever iterm opens up",
  "main": "index.js",
  "keywords": [
    "quotes",
    "iterm",
    "code",
    "inspiration"
  ],
  "author": "Imran Khan",
  "license": "ISC",
  "dependencies": {
    "fs": "*",
    "pakmanager": "^0.12.1",
    "path": "*",
    "request": "^2.81.0",
    "babel-runtime": "^6.11.6"
  },
  "browserDependencies": {},
  "scripts": {
    "compile": "rimraf lib/* && babel src -d lib",
    "prepublish": "node_modules/babel-cli/bin/babel.js src --out-dir lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.5.4"
  }
}
